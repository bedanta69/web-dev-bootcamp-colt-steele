// for of loop executes a loop that works on a source of data sourced from an itterable object
// syntax
// for (variable of iterable){ 
//    statement;}

const array1 = ['a', 'b', 'c', 'd', 'e'];

for (const element of array1) {
    console.log(element);
} 