//return values store the function values while console.log prints the value as a string which cant be used further for other purposes
//return only returns a single value
function sum(x, y) {
    return x + y; //stores the value of x+y
};
sum(1, 2)